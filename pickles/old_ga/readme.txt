the old methods, 
where the sorting is done at the end of each generation and the co passed 160 pop,
and mutation gets 160 and atlast the pop_size is made to 80

but in the new method, 
fitness sorting is done at, co, mu and only top 80 is passed in each step
